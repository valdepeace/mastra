export const Animations = {
  // Durations
  durationNormal: '200ms',
  durationSlow: '300ms',

  // Easings
  easeOut: 'cubic-bezier(0.33, 1, 0.68, 1)',
};

/** Entrance played by anything the reader watches arrive. Defined in `theme.css`. */
export const ARRIVING_CLASS = 'mastra-arriving';

/** How long that entrance runs, and so how long a word counts as new. Kept in step with `theme.css`. */
export const ARRIVING_MS = 800;
